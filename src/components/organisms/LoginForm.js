import React from "react";

const LoginForm = () => {
  return (
    <form>

      {/* GROUP of inputs / label / button */}
      {/* NAV or FORM or SIDEBAR */}

      <div>Label - Name</div>
      <div>Input - name</div>

      <div>Label - Email</div>
      <div>Input - email</div>

      <button type="submit">Submit</button>
    </form>
  );
};

export default LoginForm;
